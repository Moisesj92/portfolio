<section class="flex flex-wrap items-center justify-between gap-3 sm:gap-4">
  <div>
    <p class="text-xs uppercase tracking-[0.2em] text-emerald-700">Administración</p>
    <h1 class="mt-2 text-2xl sm:text-3xl font-semibold text-slate-900">Categorías</h1>
  </div>
  <%= link_to new_admin_category_path, class: "rounded-full bg-emerald-600 px-4 py-2 text-sm font-semibold text-white transition hover:bg-emerald-700 shadow-sm hover:shadow whitespace-nowrap" do %>
    <span class="hidden sm:inline">Nueva categoría</span>
    <span class="sm:hidden">+ Categoría</span>
  <% end %>
</section>

<div class="mt-6 sm:mt-8 grid gap-3 sm:gap-4">
  <% @categories.each do |category| %>
    <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 sm:gap-4 rounded-xl sm:rounded-2xl border border-slate-200 bg-white/80 p-4 sm:px-5 sm:py-4 shadow-sm">
      <div class="flex-1 min-w-0">
        <h2 class="text-base sm:text-lg font-semibold text-slate-900 break-words"><%= category.name %></h2>
        <p class="text-xs text-slate-500 mt-1"><%= pluralize(category.posts.count, "post") %></p>
      </div>
      <div class="flex items-center gap-2 text-xs sm:text-sm flex-wrap">
        <%= link_to "Editar", edit_admin_category_path(category), class: "rounded-full border border-slate-200 px-3 py-1.5 text-slate-700 hover:border-slate-300 hover:bg-slate-50 whitespace-nowrap" %>
        <%= button_to "Borrar", admin_category_path(category), method: :delete, data: { turbo_confirm: "¿Eliminar esta categoría?" }, class: "rounded-full border border-rose-200 px-3 py-1.5 text-rose-700 hover:border-rose-300 hover:bg-rose-50 whitespace-nowrap" %>
      </div>
    </div>
  <% end %>

  <% if @categories.empty? %>
    <div class="rounded-xl sm:rounded-2xl border border-dashed border-slate-300 bg-white/60 px-5 py-8 sm:py-10 text-center text-sm text-slate-500">
      <div class="text-3xl mb-3">🏷️</div>
      <p>No hay categorías aún. Crea la primera.</p>
    </div>
  <% end %>
</div>

<div class="mt-6 sm:mt-8">
  <%= link_to "Volver a posts", admin_posts_path, class: "inline-flex items-center gap-2 text-sm font-medium text-emerald-700 hover:text-emerald-800" %>
</div>
