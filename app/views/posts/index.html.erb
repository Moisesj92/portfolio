<section class="grid gap-4 md:gap-6 rounded-2xl md:rounded-3xl border border-amber-100 bg-white/70 p-6 md:p-8 shadow-sm backdrop-blur">
  <div>
    <p class="text-xs uppercase tracking-[0.2em] text-amber-700">Bitácora</p>
    <h1 class="mt-2 text-3xl md:text-4xl font-semibold text-slate-900">Blog</h1>
    <p class="mt-2 max-w-2xl text-sm md:text-base text-slate-600">
      Ideas, aprendizajes y proyectos en progreso. Todo lo publicado vive aquí, ordenado por fecha.
    </p>
  </div>
  <div class="flex flex-wrap items-center gap-2 md:gap-3 text-xs text-slate-500">
    <span class="rounded-full border border-slate-200 px-3 py-1.5">✨ Actualizado con cada publicación</span>
    <span class="rounded-full border border-slate-200 px-3 py-1.5">🏷️ Categorías curatoriales</span>
  </div>
</section>

<% if @posts.any? %>
  <div class="mt-8 md:mt-10 grid gap-4 md:gap-6 sm:grid-cols-2 lg:grid-cols-2">
    <% @posts.each do |post| %>
      <article class="group flex h-full flex-col justify-between rounded-2xl md:rounded-3xl border border-slate-200 bg-white/80 p-5 md:p-6 shadow-sm transition-all duration-200 hover:-translate-y-1 hover:shadow-lg hover:border-amber-200">
        <div>
          <div class="flex items-center gap-2 md:gap-3 text-xs text-slate-500">
            <span class="rounded-full border border-amber-200 bg-amber-50 px-2.5 py-1 text-amber-700 font-medium">
              <%= post.category.name %>
            </span>
            <time datetime="<%= post.published_at.iso8601 %>" class="hidden sm:block">
              <%= post.published_at.strftime("%d %b %Y") %>
            </time>
          </div>
          <h2 class="mt-3 md:mt-4 text-xl md:text-2xl font-semibold text-slate-900 leading-snug">
            <%= link_to post.title, post_path(post), class: "transition group-hover:text-amber-700" %>
          </h2>
          <p class="mt-2 md:mt-3 text-sm md:text-base text-slate-600 leading-relaxed">
            <%= truncate(post.body, length: 180) %>
          </p>
        </div>
        <div class="mt-5 md:mt-6 flex items-center gap-2 text-sm font-medium text-amber-700 group-hover:text-amber-800">
          <span>Leer artículo</span>
          <span class="transition-transform group-hover:translate-x-1">→</span>
        </div>
      </article>
    <% end %>
  </div>
<% else %>
  <div class="mt-8 md:mt-10 rounded-2xl md:rounded-3xl border border-dashed border-slate-300 bg-white/60 p-8 md:p-10 text-center text-sm md:text-base text-slate-500">
    <div class="text-4xl mb-3">📝</div>
    <p>Aún no hay publicaciones.</p>
    <p class="mt-1 text-xs">Inicia sesión en admin para crear el primer post.</p>
  </div>
<% end %>
