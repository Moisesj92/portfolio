<section class="mx-auto max-w-xl rounded-3xl border border-slate-200 bg-white/80 p-8 shadow-sm">
  <p class="text-xs uppercase tracking-[0.2em] text-amber-700">Admin access</p>
  <h1 class="mt-2 text-3xl font-semibold text-slate-900">Sign in</h1>
  <p class="mt-2 text-sm text-slate-600">Solo usuarios autorizados pueden administrar el contenido.</p>

  <%= form_with url: login_path, class: "mt-6 space-y-6" do |form| %>
    <div>
      <%= form.label :email, class: "text-xs font-semibold uppercase tracking-[0.2em] text-slate-500" %>
      <%= form.email_field :email, class: "mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm focus:border-amber-400 focus:outline-none" %>
    </div>
    <div>
      <%= form.label :password, class: "text-xs font-semibold uppercase tracking-[0.2em] text-slate-500" %>
      <%= form.password_field :password, class: "mt-2 w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm focus:border-amber-400 focus:outline-none" %>
    </div>
    <%= form.submit "Login", class: "rounded-full bg-amber-600 px-6 py-2 text-sm font-semibold text-white transition hover:bg-amber-700" %>
  <% end %>
</section>
